    
spring:

  profiles:
    active: ${FINANCE_ENV:dev}

  datasource:
    url: jdbc:h2:mem:alura
    username: sa
    password:
    driver-class-name: org.h2.Driver
    
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    
    
logging:
  level:
    root: ERROR
    '[br.com.alura.challenge.finance.backend]': DEBUG 